// import { createReducer } from '@reduxjs/toolkit';

// import { addProject, deleteProject, getProjects } from './projectOperations';

// const projectsReducer = createReducer([], {
// [getProjects.fulfilled]: (_, { payload }) => payload,
// [addProject.fulfilled]: (state, { payload }) => [...state, payload],
// //  [editContacts.fulfilled]: (state, { payload }) =>
// //   state.map(item => (item.id === payload.id ? payload : item)),

//   [deleteProject.fulfilled]: (state, { payload }) =>
//     state.filter(contact => contact.id !== payload),
//});

//export default projectsReducer;
